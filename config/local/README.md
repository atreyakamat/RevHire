# Local Development Configuration

Contains non-sensitive configuration defaults for running services in local development environments.

## Database Credentials
Configure database credentials via environment variables or a local `.env` file (ignored by git):
```properties
REVHIRE_DB_PASSWORD=<your-local-mysql-password>
```
