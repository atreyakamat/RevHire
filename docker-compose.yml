# RevHire Microservices - Local Development Orchestration Placeholder
#
# NOTE: This file establishes the target local topology for the RevHire platform.
# The complete service definitions will be finalized as services are implemented.
#
# Planned Services Topology:
# -------------------------------------------------------------
# Infrastructure:
#   - eureka-server: Service Discovery (Port: 8761)
#   - config-server: Centralized Configuration (Port: 8888)
#   - api-gateway: Spring Cloud Gateway (Port: 8080)
#
# Databases (Database-per-service architecture):
#   - mysql-user: MySQL database for User Service (Port: 3306)
#   - mysql-resume: MySQL database for Resume Service (Port: 3307)
#   - mysql-job: MySQL database for Job Service (Port: 3308)
#   - mysql-application: MySQL database for Application Service (Port: 3309)
#   - mysql-notification: MySQL database for Notification Service (Port: 3310)
#
# Business Microservices:
#   - user-service: User & Authentication Service (Port: 8081)
#   - resume-service: Resume Service (Port: 8082)
#   - job-service: Job Management Service (Port: 8083)
#   - application-service: Application Tracking Service (Port: 8084)
#   - notification-service: Notification Dispatcher (Port: 8085)
#
# Frontend:
#   - revhire-angular: Angular Web UI (Port: 4200)
# -------------------------------------------------------------

version: '3.8'

services:
  # Placeholder service to maintain valid docker-compose syntax
  revhire-placeholder:
    image: alpine:latest
    container_name: revhire-placeholder
    command: ["echo", "RevHire Microservices environment placeholder. Full stack deployment pending service implementations."]
