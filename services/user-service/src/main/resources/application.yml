server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/revhire_user?createDatabaseIfNotExist=true
    # username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
