server:
  port: 8082

spring:
  application:
    name: resume-service
  datasource:
    url: jdbc:mysql://localhost:3307/revhire_resume?createDatabaseIfNotExist=true
    # username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
