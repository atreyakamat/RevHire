server:
  port: 8084

spring:
  application:
    name: application-service
  datasource:
    url: jdbc:mysql://localhost:3309/revhire_application?createDatabaseIfNotExist=true
    # username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
