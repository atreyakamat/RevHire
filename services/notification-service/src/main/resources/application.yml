server:
  port: 8085

spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:mysql://localhost:3310/revhire_notification?createDatabaseIfNotExist=true
    # username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
